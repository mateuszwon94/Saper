#include "Results.h"
#include <array>
/*
using namespace std;

Result::Result(std::string name) : _file(name), _results() {
	string& date = string();
	int lines, columns, bombs;
	int time;
	while (!_file.eof()) {
		_file >> lines;
		_file >> columns;

	}
}

Result::~Result() { 
	_file.close();
}*/