#pragma once
/*
#include <string>
#include <map>
#include <vector>
#include <fstream>

class Result {
	public:
		Result(std::string name);
		~Result();

	private:
		std::map<std::string&, std::vector<int>> _results;
		std::fstream _file;
};
*/